{
  "scripts": {
    "build": "bun run build:scripts",
    "build:scripts": "bunx tsc && bun run tools/generate-blog-landing-pages.js",
    "generate:blog": "bun run tools/generate-blog-landing-pages.js",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "jsdom": "^27.2.0",
    "typescript": "^5.9.3"
  }
}